import { API_URL } from './constants';

export const userAuthLogin = `${API_URL}/auth/login`;
export const userAuthLogout = `${API_URL}/auth/logout`;
export const userAuthSignup = `${API_URL}/auth/signup`;
export const userAuthCheckSession = `${API_URL}/auth/check-session`;
